export interface Lesson {
  teacherId: number;
  subjectId: number;
  lessonDate: string;  
  startTime: string;
  endTime: string;
  minAge: number;
  maxAge: number;
  gender: string;
  status: string;
}